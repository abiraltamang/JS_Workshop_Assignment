// Task 3 - Create a constant and try to create another constant with the same name and see what happens, fix the mistake if something goes wrong.

const pi=3.1415;
const pi;  // SyntaxError: Identifier 'pi' has already been declared

// To fix this, we have to use different name (other than pi, which is already taken)

const a;
